apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: invoicing-service
  namespace: argocd
  labels:
    purpose: billing
spec:
  destination:
    namespace: billing
    server: https://kubernetes.default.svc
  project: default
  source:
    path: ionos_dev2/billing/invoicing-service
    repoURL: https://github.com/DOME-Marketplace/dome-gitops
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true