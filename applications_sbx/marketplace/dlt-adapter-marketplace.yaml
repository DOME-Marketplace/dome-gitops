apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dlt-adapter
  namespace: argocd
  labels:
    purpose: marketplace
spec:
  destination:
    namespace: marketplace
    server: https://kubernetes.default.svc
  project: default
  source:
    path: ionos_sbx/marketplace/connector/dlt-adapter
    repoURL: https://github.com/DOME-Marketplace/dome-gitops
    targetRevision: env_production
  syncPolicy:
    automated:
      prune: true
      selfHeal: true