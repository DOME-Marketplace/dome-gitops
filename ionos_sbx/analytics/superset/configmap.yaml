apiVersion: v1
kind: ConfigMap
metadata:
  name: superset-startup
  namespace: analytics
  labels:
    app.kubernetes.io/name: superset-analytics
    app.kubernetes.io/component: config
data:
  startup.sh: |
    #!/bin/bash
    set -e
    
    echo "Updating package lists..."
    apt-get update -qq

    echo "Installing Redis and git..."
    DEBIAN_FRONTEND=noninteractive apt-get install -y redis-server git
    
    echo "Starting Redis server..."
    redis-server --daemonize yes --bind 127.0.0.1 --port 6379 --maxmemory 256mb --maxmemory-policy allkeys-lru
    
    echo "Waiting for Redis to start..."
    sleep 5
    
    # Test Redis connection
    redis-cli ping || (echo "Redis failed to start" && exit 1)
    
    echo "Setting up Superset environment..."
    export SUPERSET_CONFIG_PATH=/app/pythonpath/superset_config.py
    export PYTHONPATH=/app/pythonpath:$PYTHONPATH
    
    # Create config directory
    mkdir -p /app/pythonpath
    
    # Create custom security manager for DOME VC claims mapping
    cat > /app/pythonpath/dome_security_manager.py << 'EOF'
    import logging
    from superset.security import SupersetSecurityManager

    logger = logging.getLogger(__name__)


    class DomeSecurityManager(SupersetSecurityManager):

        def oauth_user_info(self, provider, response=None):
            """
            Extract user info from the DOME verifier OIDC token/userinfo.
            Adjust field mappings based on what the verifier actually returns.
            """
            if provider == 'dome':
                me = self.appbuilder.sm.oauth_remotes[provider].get('oidc/userinfo')
                if me and me.ok:
                    data = me.json()
                    logger.info("DOME userinfo response: %s", data)
                    return {
                        'username': data.get('preferred_username', data.get('sub', '')),
                        'first_name': data.get('given_name', data.get('name', 'DOME')),
                        'last_name': data.get('family_name', 'User'),
                        'email': data.get('email', data.get('sub', '') + '@dome.eu'),
                        'role_keys': data.get('roles', []),
                    }
                else:
                    logger.warning("Failed to get userinfo from DOME verifier")
                    # Fallback: extract from the token itself
                    token = response or {}
                    id_token = token.get('userinfo', token.get('id_token_claims', {}))
                    logger.info("DOME token fallback data: %s", id_token)
                    return {
                        'username': id_token.get('preferred_username', id_token.get('sub', 'dome_user')),
                        'first_name': id_token.get('given_name', id_token.get('name', 'DOME')),
                        'last_name': id_token.get('family_name', 'User'),
                        'email': id_token.get('email', id_token.get('sub', 'user') + '@dome.eu'),
                        'role_keys': id_token.get('roles', []),
                    }
            return {}
    EOF
    
    # Create superset config with DOME OAuth integration
    cat > /app/pythonpath/superset_config.py << 'EOF'
    import os
    from dome_security_manager import DomeSecurityManager
    from flask_appbuilder.security.manager import AUTH_OAUTH
    
    # Database - using SQLite with persistent storage
    SQLALCHEMY_DATABASE_URI = 'sqlite:////app/data/superset.db'
    
    # Redis configuration
    REDIS_HOST = '127.0.0.1'
    REDIS_PORT = 6379
    
    # Celery configuration
    CELERY_CONFIG = {
        'broker_url': f'redis://{REDIS_HOST}:{REDIS_PORT}/0',
        'result_backend': f'redis://{REDIS_HOST}:{REDIS_PORT}/0',
        'worker_log_level': 'INFO',
        'worker_prefetch_multiplier': 1,
        'task_acks_late': True,
    }
    
    # Cache configuration
    CACHE_CONFIG = {
        'CACHE_TYPE': 'RedisCache',
        'CACHE_REDIS_HOST': REDIS_HOST,
        'CACHE_REDIS_PORT': REDIS_PORT,
        'CACHE_REDIS_DB': 1,
        'CACHE_DEFAULT_TIMEOUT': 300,
    }
    
    # Security
    SECRET_KEY = 'ci8q7ZczfuWjshBKv8ybRC3ADn9a6FUX'
    WTF_CSRF_ENABLED = False

    # Enable proxy fix for running behind ingress/load balancer
    ENABLE_PROXY_FIX = True
    PROXY_FIX_CONFIG = {"x_for": 1, "x_proto": 1, "x_host": 1, "x_port": 0, "x_prefix": 1}

    # Authentication type - OAuth (uses Authlib)
    AUTH_TYPE = AUTH_OAUTH

    # DOME Verifier OAuth configuration (public client, no client_secret)
    OAUTH_PROVIDERS = [
        {
            'name': 'dome',
            'icon': 'fa-address-card',
            'token_key': 'access_token',
            'remote_app': {
                'client_id': 'did:key:zDnaer6wstrAhZxM5ej7fvQbnf9aP3RdojhXEao431SMrN3Lb',
                'server_metadata_url': 'https://verifier.dome-marketplace-sbx.org/.well-known/openid-configuration',
                'client_kwargs': {
                    'scope': 'openid_learcredential',
                    'token_endpoint_auth_method': 'none',
                },
                'api_base_url': 'https://verifier.dome-marketplace-sbx.org/',
            },
        }
    ]

    # Custom security manager for DOME VC claims
    CUSTOM_SECURITY_MANAGER = DomeSecurityManager
    
    # User registration settings
    AUTH_USER_REGISTRATION = True
    AUTH_USER_REGISTRATION_ROLE = 'Gamma'
    
    # Anonymous user role
    AUTH_ROLE_PUBLIC = "Public"
    
    # Sync roles at each login
    AUTH_ROLES_SYNC_AT_LOGIN = True
    
    # Feature flags
    FEATURE_FLAGS = {
        'ENABLE_TEMPLATE_PROCESSING': True,
    }
    
    # Load examples
    SUPERSET_LOAD_EXAMPLES = True
    
    # Webserver config
    SUPERSET_WEBSERVER_PORT = 8088
    SUPERSET_WEBSERVER_ADDRESS = '0.0.0.0'
    
    WEBDRIVER_BASEURL = 'https://analytics.dome-marketplace-sbx.org/'
    
    # SIP 15 support
    SIP_15_ENABLED = True
    EOF
    
    # Ensure data directory exists
    mkdir -p /app/data
    
    echo "Installing required packages..."
    # Install packages - Authlib instead of flask-oidc
    pip install --no-cache-dir \
        'crate[sqlalchemy]==0.26.0' \
        Authlib==1.2.1 \
        sqlalchemy_trino==0.4.1 \
        sqlalchemy-drill==1.1.2 \
        mysqlclient==2.1.0 \
        pymssql==2.2.5 \
        || echo "Package installation completed with warnings"
    
    echo "Initializing Superset database..."
    superset db upgrade
    
    echo "Creating admin user..."
    superset fab create-admin \
        --username admin \
        --firstname Admin \
        --lastname User \
        --email admin@example.com \
        --password admin 2>/dev/null || echo "Admin user may already exist"
    
    echo "Loading examples..."
    superset load_examples 2>/dev/null || echo "Examples may already be loaded"
    
    echo "Initializing Superset..."
    superset init
    
    echo "Starting Celery worker in background..."
    nohup celery --app=superset.tasks.celery_app:app worker --loglevel=info > /tmp/celery.log 2>&1 &
    
    echo "Waiting for Celery to start..."
    sleep 10
    
    echo "Starting Superset web server..."
    exec superset run -h 0.0.0.0 -p 8088 --with-threads
