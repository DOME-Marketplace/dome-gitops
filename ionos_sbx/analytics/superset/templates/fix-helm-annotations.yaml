apiVersion: v1
kind: ConfigMap
metadata:
  name: superset-fix-annotations
  namespace: analytics
  annotations:
    meta.helm.sh/release-name: superset
    meta.helm.sh/release-namespace: analytics
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-delete-policy": before-hook-creation
data:
  dummy: "This is a dummy configmap to fix Helm ownership validation errors."
