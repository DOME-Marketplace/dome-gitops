apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bacula-db-pvc
  namespace: bacula
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ionos-enterprise-hdd
  resources:
    requests:
      storage: 5Gi
