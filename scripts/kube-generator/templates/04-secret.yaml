apiVersion: v1
kind: Secret
type: kubernetes.io/service-account-token
metadata:
  name: %NAMESPACE%-sa-token-%ENV%
  namespace: %NAMESPACE%
  annotations:
    kubernetes.io/service-account.name: %NAMESPACE%-sa-%ENV%